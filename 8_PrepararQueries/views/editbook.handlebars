<h1>Edite o Livro</h1>

<form action="/books/update/{{book.idbooks}}" method="POST">
    <div class="formControl">
        <div>
            <label for="nomeLivro">Nome do Livro</label>
            <input
                type="text"
                id="nomeLivro"
                name="nomeLivro"
                value="{{book.title}}"
                required
            />
        </div>
        <div>
            <label for="nomeAutor">Nome do Autor</label>
            <input
                type="text"
                id="nomeAutor"
                name="nomeAutor"
                value="{{book.author}}"
                required
            />
        </div>
        <div>
            <label for="qtdPaginas">Quandidade de Páginas</label>
            <input
                type="number"
                id="qtdPaginas"
                name="qtdPaginas"
                value="{{book.pages}}"
                required
            />
        </div>
        <button type="submit">Editar</button>

    </div>

</form>